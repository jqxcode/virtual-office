{
  "jobs": {
    "detect-hang": {
      "prompt": "Run hang detection scan now. Read the 'hangDetection' config from this job definition for per-agent thresholds. Scan all VO agent lock files under Q:/src/personal_projects/virtual-office/state/agents/*/lock. For each lock, check if the age exceeds that agent's hangThresholdMinutes (default 60). For hung jobs: walk the process tree, capture py-spy stack traces for Python processes, classify the hang, decide whether to kill (only if age > killThresholdMinutes), log the incident, and file GitHub issues for new patterns. Skip any agents listed in 'exclude'. Output a scan summary at the end.",
      "maxRuns": 0,
      "enabled": true,
      "saveOutput": true,
      "description": "Scans for hung VO agent jobs, diagnoses via process tree and py-spy, kills if safe, logs incidents",
      "hangDetection": {
        "defaultHangThresholdMinutes": 60,
        "defaultKillThresholdMinutes": 120,
        "exclude": ["hang-scout"],
        "perAgent": {
          "scrum-master": {
            "hangThresholdMinutes": 60,
            "killThresholdMinutes": 120
          },
          "bug-killer": {
            "hangThresholdMinutes": 90,
            "killThresholdMinutes": 180
          },
          "emailer": {
            "hangThresholdMinutes": 30,
            "killThresholdMinutes": 60
          },
          "memo-checker": {
            "hangThresholdMinutes": 20,
            "killThresholdMinutes": 30
          },
          "poster": {
            "hangThresholdMinutes": 15,
            "killThresholdMinutes": 30
          }
        }
      }
    },
    "detect-scrum-master": {
      "prompt": "Run hang detection scan targeting ONLY the scrum-master agent. Use hangThresholdMinutes=30 and killThresholdMinutes=60 for this scan (override the config defaults). Scan ONLY the lock file at Q:/src/personal_projects/virtual-office/state/agents/scrum-master/lock. If age >= 30 minutes: walk the process tree, capture py-spy stack traces for Python processes, classify the hang, decide whether to kill (only if age > 60 min), log the incident, and file GitHub issues for new patterns. Output scan summary.",
      "maxRuns": 0,
      "enabled": true,
      "saveOutput": true,
      "description": "One-off hang detection for scrum-master only with 20min hang / 60min kill thresholds"
    },
    "daily-report": {
      "prompt": "Generate the daily 5W incident report. (1) Parse audit logs from Q:/src/personal_projects/virtual-office/output/audit/*.jsonl for the past 7 days to extract run durations per agent/job. (2) Collect all hang incidents from Q:/src/personal_projects/virtual-office/output/hang-scout/incidents/ in the last 24 hours. (3) Generate a self-contained dark-themed HTML report with: executive summary, 5W incident details, pattern summary table, a Chart.js line chart showing runtime trends per agent/job over 7 days (include the chart.js CDN script and adapter), a runtime summary table with avg/max/trend, and recommendations (flag increasing trends and jobs approaching hang thresholds). Save to output/hang-scout/daily-report-YYYYMMDD.html and daily-report-latest.html. Open in Edge.",
      "maxRuns": 0,
      "enabled": true,
      "saveOutput": false,
      "description": "Generates daily 5W incident report summarizing all hang detections in the last 24 hours"
    }
  }
}

{
  "jobs": {
    "approve-CoreIdentity": {
      "prompt": "Run the CoreIdentity approval and renewal script. Execute: python ${LOCAL_SKILLS}/data_tools/coreidentity-approve.py. This handles: (1) approving pending entitlement requests from team members via CoreIdentity/MSApprovals, (2) approving pending UMS requests at ums.microsoft.com, (3) approving pending M365 Pulse CorpIdentity requests at m365pulse.microsoft.com/CorpIdentity/RequestApproval, (4) approving pending IDWeb requests at idweb.microsoft.com (security group membership approvals), and (5) renewing your own expiring memberships. Report the results summary printed by the script. If the script reports errors connecting to CDP, note that headless Edge may need to be relaunched. IMPORTANT: If any requests show status 'no_approve_button' or 'Card click did not open detail panel', re-run the script once more -- transient CDP rendering can cause false negatives. If failures persist after retry, report the exact request IDs and error status so they can be manually approved.",
      "cron": "0 8 * * *",
      "maxRuns": 0,
      "saveOutput": true,
      "description": "Approve pending entitlement requests and renew own expiring memberships daily"
    },
    "approve-FFv2": {
      "prompt": "Run the FFv2 approval script with the following extra arguments (may be empty): [{{EXTRA_ARGS}}]. You MUST invoke the script via Bash or powershell script now -- do not skip, do not claim the work was already done, do not assume a prior turn ran it. There is no prior turn. Command to execute: `python ${LOCAL_SKILLS}/data_tools/ffv2-approve.py {{EXTRA_ARGS}}` (when extra args is empty, just run the script with no arguments). The script auto-detects argument type: no args = approve all directs' pending flights, a number = flight ID, a URL containing featureFlight/ = extract ID, anything else = person name. After the script exits, report the results summary it printed.",
      "maxRuns": 0,
      "saveOutput": true,
      "description": "Approve all pending FFv2 flights from directs (daily 9:33pm), or targeted by person/ID/URL"
    }
  }
}
